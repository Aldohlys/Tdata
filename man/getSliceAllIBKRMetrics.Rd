% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ibkr.R
\name{getSliceAllIBKRMetrics}
\alias{getSliceAllIBKRMetrics}
\title{getSliceAllIBKRMetrics}
\usage{
getSliceAllIBKRMetrics(first = 1, last = 0)
}
\arguments{
\item{first}{Numeric, start index of the ticker slice to process. Default is 1 (first ticker).}

\item{last}{Numeric, end index of the ticker slice to process. Default is 0, which processes all tickers.
When specified, tickers from index first to last will be processed.}
}
\value{
A data frame containing combined metrics data for all processed tickers. Each row
represents data returned by \code{getIBKRMetrics()} for a single ticker.
}
\description{
Retrieve Market Data for a Subset of Tickers from IBKR TWS API
}
\details{
This function retrieves market data metrics for a specified slice of tickers from Interactive Brokers
Trader Workstation (TWS) API. It filters tickers based on exchange criteria and processes each ticker
sequentially to gather metrics.

The function performs the following operations:
\enumerate{
\item Retrieves all available tickers using \code{getAllTickers()}
\item Optionally slices the ticker list based on parameters first and last
\item Filters tickers to include only those from exchanges "SMART", "EUREX", or "CBOE"
\item Processes each filtered ticker sequentially using \code{getIBKRMetrics()}
\item Combines all results into a single data frame
}

The function outputs progress messages indicating:
\itemize{
\item Which ticker range is being processed
\item The list of tickers before filtering
\item The list of tickers after filtering by exchange
}
}
\note{
This function requires an active connection to IBKR TWS API. The underlying \code{getIBKRMetrics()}
function handles the actual data retrieval.
}
\examples{
# Process all tickers
all_metrics <- getSliceAllIBKRMetrics()

# Process only the first 5 tickers
first_five <- getSliceAllIBKRMetrics(last=5)

# Process tickers from index 10 to 20
subset_metrics <- getSliceAllIBKRMetrics(first=10, last=20)

}
\seealso{
\code{\link{getAllTickers}} for retrieving the complete list of available tickers
\code{\link{getIBKRMetrics}} for retrieving metrics for a single ticker
}
